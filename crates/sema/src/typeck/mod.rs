/*
use crate::{
    hir,
    ty::{Gcx, Ty, TyKind},
};
use solar_data_structures::map::FxHashMap;

struct TypeCollector<'gcx> {
    gcx: Gcx<'gcx>,
    map: FxHashMap<hir::ItemId, Ty<'gcx>>,
}

impl<'gcx> TypeCollector<'gcx> {
    pub fn new(gcx: Gcx<'gcx>) -> Self {
        Self { gcx, map: FxHashMap::default() }
    }

    pub fn visit(&mut self, id: hir::ItemId) {
        todo!()
    }
}
*/
