//@ compile-flags: -Zmir-pipeline=default -Zprint-after-stage
//@ filecheck:
@module PhaseLibraryResume
@library
@phase abi

// CHECK: @library
// CHECK-LABEL: checkpoint=mir.dispatch
// CHECK: @phase dispatch
// CHECK-NOT: fn @entry
// CHECK-LABEL: checkpoint=mir.intrinsics-lowered
fn @helper(arg0: u256) -> u256 {
  bb0:
    ret arg0
}
