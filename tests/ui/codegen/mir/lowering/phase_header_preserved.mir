//@compile-flags: -Zmir-pipeline=default -Zprint-after-stage
//@filecheck: --match-full-lines
// Re-running the default pipeline on intrinsic-lowered input completes the progressive
// lowering pipeline instead of rejecting the transition.
// CHECK-LABEL: {{.*}}checkpoint=mir.evm-shaped{{.*}}
// CHECK: @module PhaseHeaderPreserved
// CHECK-NEXT: @library
// CHECK-NEXT: @phase evm-shaped
/// Module docs.
@module PhaseHeaderPreserved
@library
@phase intrinsics-lowered
fn @f() {
  bb0:
    stop
}
