//@compile-flags: --pass dce
// `built` is the default phase: an explicit `[phase = built]` parses but is
// not printed back.
; module @PhaseExplicitBuilt [phase = built]
fn @f() {
  bb0 (entry):
    stop
}
