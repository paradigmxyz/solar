//@compile-flags: --pass jump-threading
//@filecheck:
// A block whose only instruction is a phi is not an empty forwarder: threading
// its predecessors past it would sever the phi's incoming edges.
@module JumpThreadingPhiForwarder
// CHECK-LABEL: {{^[ +].*}}fn @keep_phi_only_block{{[( ]}}
// CHECK: {{^[ +].*}}jumpi arg0, bb1, bb2
// CHECK: {{^[ +].*}}[[VALUE:v[0-9]+]] = phi [bb1: 1], [bb2: 2]
// CHECK: {{^[ +].*}}add [[VALUE]], 1
fn @keep_phi_only_block(arg0: bool) -> u256 {
  bb0:
    jumpi arg0, bb1, bb2
  bb1:
    jump bb3
  bb2:
    jump bb3
  bb3:
    v2 = phi [bb1: 1], [bb2: 2]
    jump bb4
  bb4:
    v3 = add v2, 1
    ret v3
}
