//@ failure-status: 1
// Once operands are cleared, undefined opcodes must carry an explicit
// stack effect. Otherwise the verifier cannot model their runtime pops. Keep
// checking after the first invalid instruction so both diagnostics are emitted.
bb0 (entry):
  %r = op_0c
  %s = op_0d
  stop
