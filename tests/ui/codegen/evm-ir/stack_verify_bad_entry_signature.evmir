//@ failure-status: 1
// bb0 exits with [%b, %a] (top first), so bb1 cannot declare `%a` as the top
// incoming stack word.
@module module

bb0 (entry):
  %a = push 1
  %b = push 2
  jump bb1
bb1 (in %a):
  %c = add %a, %a
  stop
