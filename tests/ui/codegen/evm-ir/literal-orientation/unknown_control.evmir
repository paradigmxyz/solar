//@ revisions: byzantium osaka
//@ compile-flags: -Ogas -Zevm-ir-pipeline=literal-orientation
//@[byzantium] compile-flags: --evm-version byzantium
//@[osaka] compile-flags: --evm-version osaka
//@ filecheck:
@module LateOrientationUnknownControl

// The numeric destination is unknown independently of any public label.
// CHECK-LABEL: {{^[ +].*}}bb0:
// CHECK: {{^[ +].*}}push 7
// CHECK-NEXT: {{^[ +].*}}swap 1
// CHECK-NEXT: {{^[ +].*}}swap 2
// CHECK-NEXT: {{^[ +].*}}add
// CHECK: {{^[ +].*}}jump
bb0:
  push 11
  push 12
  push 7
  swap 1
  swap 2
  add
  push 0
  calldataload
  jump
