//@filecheck: --implicit-check-not=!meta
@module module

// CHECK-LABEL: bb0:
// CHECK-COUNT-5: push 0
// CHECK: stop
bb0:
  push 0 !meta(type=u256)
  push 0 !meta(ty=u256)
  push 0 !meta(result_ty=u256)
  push 0 !meta(mir_type=u256)
  push 0 !meta(marker)
  stop
