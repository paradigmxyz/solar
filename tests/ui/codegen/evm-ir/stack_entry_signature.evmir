//@compile-flags: --pass stack-schedule
// A block can declare its incoming stack signature with `(in ...)`; scheduling
// seeds the model stack from it so blocks that consume predecessor values are
// scheduled instead of bailing.
bb0 (entry):
  %a = push 5
  jump bb1
bb1 (in %a):
  %b = add %a, %a
  stop
