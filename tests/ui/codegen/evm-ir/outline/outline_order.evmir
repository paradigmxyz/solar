//@compile-flags: --pass outline
//@filecheck:
@module module

// CHECK-LABEL: {{^[ +].*}}bb0:
// CHECK: + push [[RET0:bb[0-9]+]]
// CHECK: + jump [[OUTLINE:bb[0-9]+]]
// CHECK: + bb1:
// CHECK: + push [[RET1:bb[0-9]+]]
// CHECK: + jump [[OUTLINE]]
// CHECK: + bb2:
// CHECK: {{^[ +].*}}push 2
// CHECK: + [[OUTLINE]]:
// CHECK: {{^[ +].*}}push 1
// CHECK: {{^[ +].*}}push 12
// CHECK: + jump
// CHECK: + [[RET0]]:
// CHECK: {{^[ +].*}}push 13
// CHECK: + [[RET1]]:
bb0:
  push 1
  pop
  push 2
  pop
  push 3
  pop
  push 4
  pop
  push 5
  pop
  push 6
  pop
  push 7
  pop
  push 8
  pop
  push 9
  pop
  push 10
  pop
  push 11
  pop
  push 12
  pop
  push 13
  pop
  stop
bb1:
  push 1
  pop
  push 2
  pop
  push 3
  pop
  push 4
  pop
  push 5
  pop
  push 6
  pop
  push 7
  pop
  push 8
  pop
  push 9
  pop
  push 10
  pop
  push 11
  pop
  push 12
  pop
  stop
bb2:
  push 2
  pop
  push 3
  pop
  push 4
  pop
  push 5
  pop
  push 6
  pop
  push 7
  pop
  push 8
  pop
  push 9
  pop
  push 10
  pop
  push 11
  pop
  push 12
  pop
  push 13
  pop
  stop
