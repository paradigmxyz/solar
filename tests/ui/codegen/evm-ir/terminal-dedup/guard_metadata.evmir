//@ compile-flags: -Zevm-ir-pipeline=terminal-dedup

// Explicit instruction metadata remains part of terminal-body identity.
@module TerminalMetadata
bb0:
  push 1
  indexed_jump bb1, bb2
bb1:
  push 0 !meta(stack=0->1)
  push 0
  push 0
  revert
bb2:
  push 0
  push 0
  push 0
  revert
