//@ compile-flags: -Ogas -Zevm-ir-pipeline=pack-data
//@ filecheck:
@module subslices

// CHECK-LABEL: {{^[ +].*}}bb0:
// CHECK: {{^[ +].*}}push_data literal_0+1
// CHECK: {{^[ +].*}}push_data literal_0+2
// CHECK: {{^[ +].*}}push_data literal_0+1
bb0:
  push_data literal_0
  pop
  push_data literal_1
  pop
  push_data literal_2+1
  pop
  stop

// CHECK: @data literal_0 hex"00112233445566"
// CHECK-NOT: @data literal_1
@data literal_0 hex"1122334455"
@data literal_1 hex"2233"
@data literal_2 hex"00112233445566"
