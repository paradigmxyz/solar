//@compile-flags: --pass stack-schedule
// RETURN(offset, size) wants the offset on top, then size below it. The two
// pushes leave size on top, so the scheduler emits a `swap1` to bring the
// offset to the top before the terminator.
bb0 (entry):
  %off = push 0
  %size = push 32
  return %off, %size
