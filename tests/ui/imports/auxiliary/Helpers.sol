// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

library Helper {
    function doSomething() public pure returns (uint) {
        return 1;
    }
}

library Utils {
    function utilFunc() public pure returns (uint) {
        return 2;
    }
}