//@ compile-flags: -Zcodegen
//@ filecheck:
// CHECK: "errors": [
// CHECK: "severity": "error"
// CHECK: "message": "codegen does not support `fallback(bytes) returns (bytes)` yet"
// CHECK-NOT: "contracts"
{
  "language": "Solidity",
  "sources": {
    "Fallback.sol": {
      "content": "contract C { fallback(bytes calldata input) external returns (bytes memory) { return input; } }"
    }
  },
  "settings": {
    "outputSelection": {
      "*": {
        "*": [
          "evm.bytecode.object",
          "evm.deployedBytecode.object"
        ]
      }
    }
  }
}
