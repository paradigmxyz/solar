Scripts and tools for fuzzing the compiler.

Run stuff in here by first `cd`ing into this directory (`cd fuzz`).

See `fandango/` for a local-only prototype that generates ABI-value vectors
for runtime differential testing.
